server.port=8888

# Use the following only if the directory is a git repo; otherwise, use native.searchLocations
#spring.cloud.config.server.git.uri=file://${user.dir}/../chapter5-config-repo

spring.profiles.active=native
# In Windows, need an extra "/" in the file URL if it is absolute with a drive prefix: file:///
spring.cloud.config.server.native.searchLocations=file://${user.dir}/../chapter5-config-repo
